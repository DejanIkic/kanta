# FastAPI i web server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Serijska komunikacija
pyserial==3.5

# Database i ORM
sqlalchemy==2.0.23
alembic==1.12.1

# Data validacija i settings
pydantic==2.5.0
pydantic-settings==2.1.0

# Logging
structlog==23.2.0

# Development i testiranje
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Monitoring i health checks
python-multipart==0.0.6

#debug
debugpy

# Smart Bin - Image classification
numpy>=1.24.0
opencv-python-headless>=4.8.0

# TFLite - graceful: try/except import, falls back to MockClassifier
# On RPi5 ARM64: pip install tflite-runtime
# tflite-runtime>=2.13.0

# picamera2 - apt install only on RPi5: sudo apt install python3-picamera2
# Not available via pip - try/except import in code
